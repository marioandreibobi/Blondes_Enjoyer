import React from "react";

export default function BlueprintGrid(): React.ReactElement {
  return (
    <div
      className="fixed inset-0 z-0 pointer-events-none bg-grid-blueprint"
      aria-hidden="true"
    />
  );
}
